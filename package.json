{
  "name": "sparkta",
  "version": "0.1.0",
  "private": true,
  "description": "Local agent-powered rapid UI prototyping foundation",
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "concurrently --kill-others --success first --names web,server --prefix-colors cyan,magenta npm:dev:web npm:dev:server",
    "dev:web": "npm run dev --workspace @sparkta/web",
    "dev:server": "npm run dev --workspace @sparkta/server",
    "format:check": "prettier --check package.json tsconfig.base.json eslint.config.js prettier.config.js apps templates/default README.md docs project/architecture/README.md project/work-items/5-establish-the-blessed-frontend-starter project/work-items/6-codify-ui-generation-instructions-and-quality-checks project/work-items/7-run-repeated-prototype-0-generation-trials",
    "lint": "eslint apps",
    "test": "npm run test --workspaces --if-present",
    "test:focused": "vitest run",
    "type-check": "npm run type-check --workspaces --if-present"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jsburckhardt/sparkta.git"
  },
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "concurrently": "^10.0.4",
    "eslint": "^10.8.0",
    "globals": "^17.9.0",
    "prettier": "^3.9.6",
    "typescript-eslint": "^8.66.1-alpha.4",
    "vitest": "^4.1.10"
  }
}
