# Backpressure Coverage — Run repeated Prototype 0 generation trials

**Plan**: [01-action-plan.md](../01-action-plan.md)
**Basis (plan SHA-256)**: `5d593dae5a8636bf5fb7b805e2e422c8b2fd2b6217607cc0397766807736b783`
**Generated**: 2026-08-18
**Certainty**: Weak

> Advisory only. Never blocks, never gates, and emits no score.
> Selection, not enforcement: no proof below executes automatically at phase end.

## Existing Sensors (inventory)

| Sensor                                       | Paved command                                | Dimension                   | Found in                                                                                          |
| -------------------------------------------- | -------------------------------------------- | --------------------------- | ------------------------------------------------------------------------------------------------- |
| Canonical starter contract and runtime smoke | `just starter-check`                         | behaviour                   | Root `justfile`, exercising `templates/default/`                                                  |
| Workspace unit tests                         | `just test`                                  | behaviour                   | Root command over `apps/server` and `apps/web` Vitest suites                                      |
| Focused repository verification              | `harness checks focused [target] --json`     | maintainability / behaviour | `.harness/extensions/checks`, delegating to root `just verify-focused`                            |
| Static workspace checks                      | `just lint`, `just type-check`, `just build` | maintainability             | Root scripts over `apps/server` and `apps/web`                                                    |
| Full repository verification                 | `harness checks full --json`                 | maintainability / behaviour | `.harness/extensions/checks`, delegating to root `just verify` and including `just starter-check` |

Surveyed roots: repository root, npm workspace roots `apps/server` and `apps/web`, standalone package root `templates/default`, and common recursive roots `.harness`, `harness`, `e2e`, `tests`, `test`, `packages`, and `crates`. The filesystem sweep found no CI workflow, browser/DOM E2E configuration, component-DOM dependency, architecture analyzer, or trial-specific command.

## Coverage Matrix

| Criterion / failure mode                                                                                                            | Phase | Selected proof                                                                                                                                                                                    | Status    | Tier                          | Probe trail (required if ABSENT)                                                                                                                                                                                                                                                              |
| ----------------------------------------------------------------------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ----------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Canonical starter copy/build/runtime/isolation guarantees regress before trials                                                     | 1     | RUN: `just starter-check`                                                                                                                                                                         | EXISTS    | computational                 | —                                                                                                                                                                                                                                                                                             |
| AC-1 — exactly three fixed clean copies receive their prompts and successful generation creates a non-empty app-only source diff    | 1     | BUILD→RUN: Phase 0 adds `trial-init`, `trial-generate`, and trial evidence validation; then `just trial-init <trial> <attempt>`, `just trial-generate <trial> <attempt>`, and `just trials-check` | BUILDABLE | computational                 | —                                                                                                                                                                                                                                                                                             |
| AC-2 — every attempt has the seven required records with finite, safe outcomes                                                      | 1     | BUILD→RUN: Phase 0 adds the all-trial record/schema checker; then `just trials-check`                                                                                                             | BUILDABLE | computational                 | —                                                                                                                                                                                                                                                                                             |
| AC-3 — each generated app remains simulated/frontend-only, installs reproducibly, builds, serves on an assigned port, and cleans up | 1     | BUILD→RUN: Phase 0 generalizes the existing starter mechanics into an attempt validator; then `just trial-validate <trial> <attempt>` and `just trials-check`                                     | BUILDABLE | computational                 | —                                                                                                                                                                                                                                                                                             |
| AC-4 — every prompt-major control and checklist row has a complete source/effect verdict and every unmet check is indexed           | 1     | BUILD→RUN: Phase 0 adds completeness and finite-outcome rules to the all-trial checker; then `just trials-check`                                                                                  | BUILDABLE | computational                 | —                                                                                                                                                                                                                                                                                             |
| AC-4 — controls actually behave plausibly when rendered and the visual/responsive result is contextually appropriate                | 1     | —                                                                                                                                                                                                 | ABSENT    | inferential / human-judgement | Searched recursive Playwright, Cypress, Puppeteer/CDP, `*.spec.*`, `*.e2e.*`, jsdom, happy-dom, and Testing Library signatures across root, both `apps/*` workspaces, `templates/default`, `.harness`, and common test/e2e/package roots; none provides rendered interaction or visual proof. |
| AC-5 — every finding has a disposition and each adopted change has exactly the affected clean reruns with comparable evidence       | 1     | BUILD→RUN: Phase 0 adds findings-graph, attempt-bound, prompt-hash, and rerun-set rules; then `just trials-check`                                                                                 | BUILDABLE | computational                 | —                                                                                                                                                                                                                                                                                             |
| AC-6 — three latest-attempt verdicts compose into the fixed overall verdict and every failure names an exact blocker                | 1     | BUILD→RUN: Phase 0 adds verdict arithmetic and blocker checks; then `just trials-check`                                                                                                           | BUILDABLE | computational                 | —                                                                                                                                                                                                                                                                                             |
| AC-6 — the generated prototypes visually represent each request, avoid unfinished-scaffold quality, and are useful                  | 1     | —                                                                                                                                                                                                 | ABSENT    | inferential / human-judgement | Searched the same browser/DOM/visual signatures across all surveyed roots plus screenshot and visual-diff signatures; no sensor can determine contextual usefulness or design taste.                                                                                                          |

## Proof Plan (selected)

### Phase 1: Trial protocol, execution, evaluation, and closeout

| Proves                                                  | Mode      | Proof line                                                                                                                                                                           |
| ------------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Canonical starter baseline                              | RUN       | `just starter-check`                                                                                                                                                                 |
| Three fixed generated copies                            | BUILD→RUN | Build `trial-init`, `trial-generate`, and trial evidence validation; then run `just trial-init <trial> <attempt>`, `just trial-generate <trial> <attempt>`, and `just trials-check`. |
| Complete per-attempt records                            | BUILD→RUN | Build the record/schema checker; then run `just trials-check`.                                                                                                                       |
| Generated-app isolation, build, runtime, and cleanup    | BUILD→RUN | Build the per-attempt operational validator; then run `just trial-validate <trial> <attempt>` and `just trials-check`.                                                               |
| Control/checklist evidence completeness                 | BUILD→RUN | Add control-table, checklist, and unmet-check rules; then run `just trials-check`.                                                                                                   |
| Findings and bounded reruns                             | BUILD→RUN | Add disposition, affected-set, attempt-bound, and comparison rules; then run `just trials-check`.                                                                                    |
| Final verdict arithmetic and blockers                   | BUILD→RUN | Add latest-attempt, overall-verdict, and blocker rules; then run `just trials-check`.                                                                                                |
| Repository regression closeout                          | RUN       | `harness checks full --json`                                                                                                                                                         |
| Rendered control plausibility and contextual usefulness | ABSENT    | Perform the bounded source-backed and human quality review already specified by V-4 and V-6; do not label it automated proof.                                                        |

Selected current commands are `just starter-check`, `harness checks focused [target] --json`, and `harness checks full --json`. Proposed Phase 0 commands are `just trial-init <trial> <attempt>`, `just trial-generate <trial> <attempt>`, `just trial-validate <trial> <attempt>`, and `just trials-check`.

## Certainty: Weak

Counts (behaviour/architecture rows): **1 RUN · 0 EXTEND · 6 BUILD · 2 ABSENT**

Recommended next move (per-task lookup, advisory): build the risk-linked trial checks before feature execution, and pause for explicit acceptance of the remaining rendered-quality inference tax.

Only the canonical starter baseline has runnable deterministic proof today. Six trial-specific promises need newly paved checks, while rendered behavior and contextual usefulness remain material human judgments.

## Recommended Phase 0: Establish Backpressure (build or extend)

| Sensor to build/extend                     | Proves                                      | Suggested form                                                                                                                                                           | Paved command it strengthens/exposes                                         |
| ------------------------------------------ | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------- |
| Trial initialization and generation guards | AC-1 and bounded portions of AC-2           | Root recipes with allowlisted IDs, overwrite refusal, clean-baseline hashes, finite Copilot execution, app-only diffing, and non-empty generated-source proof on success | `just trial-init <trial> <attempt>`; `just trial-generate <trial> <attempt>` |
| Per-attempt operational validator          | AC-2 and AC-3                               | Reuse the owned-process, assigned-port, HTTP marker, lock, build, isolation, and cleanup mechanics already proven by `starter-check`, but target one persistent attempt  | `just trial-validate <trial> <attempt>`                                      |
| Cross-trial evidence checker               | Deterministic portions of AC-1 through AC-6 | Static schema/graph checker for required records, finite outcomes, control/checklist completeness, finding dispositions, rerun bounds, verdict arithmetic, and blockers  | `just trials-check`                                                          |

A future real-browser interaction sensor could reduce the source-backed inference tax, but it would not determine whether a design is contextually useful or tasteful. Browser automation remains outside this plan unless the owner explicitly expands scope.

## Closing Verdict

One thing I already did, automatically: I surveyed every package root and wrote the exact current and proposed proof commands beside each promise, so a later implementer can see what green must mean without reconstructing this conversation. The existing starter check can prove only that the neutral starter still installs, builds, serves, and cleans up; if that check passes but the starter is broken, fix the checker first, then the code.

One thing I changed because the survey found missing planned proof: the three plan documents now name the proposed trial commands and require a successful generation to leave a non-empty app-only source diff. I'd like your OK to build those root checks before running the trials, because the repository currently has no deterministic trial sensor. Even after those checks exist, whether controls feel plausible when rendered and whether each prototype is genuinely useful still needs bounded human review; no current command can make that judgment.

In summary: `just starter-check`, the proposed trial commands, `just trials-check`, and `harness checks full --json` will prove the starter, trial records, operational contract, rerun graph, and verdict arithmetic. Human review will still decide rendered plausibility and contextual usefulness; approve the proposed Phase 0 checks and the explicit remaining inference tax before trial execution.
