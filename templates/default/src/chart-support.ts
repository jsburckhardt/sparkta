// Re-export the bundled chart primitives generated interfaces commonly need.
// The neutral starter page stays chart-free while this module proves resolution.
export { Bar, BarChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
